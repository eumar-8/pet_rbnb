<div class="container">
<%= simple_form_for current_user, url: profile_path(current_user) do |f| %>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.input :email %>
  <%= f.input :name %>
  <%= f.input :location %>
  <%= f.button :submit %>

<% end %>
</div>
