<h1>Pets Index</h1>
<div class="container">
  <div class="row">
    <% @pets.each do |pet| %>
      <div class="col-xs-12 col-sm-4">
        <%= render partial: 'shared/pet-card', locals: { pet: pet } %>
      </div>
    <% end %>
  </div>
</div>
